# frozen_string_literal: true

RSpec.describe Phimg do
  it "has a version number" do
    expect(Phimg::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
